{
  "name": "codex-model-router",
  "version": "0.0.0",
  "private": true,
  "description": "開發用設定檔。發佈的成品是 codex-model-router.sh / .ps1 兩支單檔安裝器，由 src/ 經 tools/build.mjs 產生，不經 npm 安裝。",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "sync": "node tools/build.mjs",
    "lint": "eslint .",
    "test": "node --test \"test/*.test.mjs\"",
    "check": "node tools/build.mjs --check && eslint . && npm test"
  },
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "globals": "^17.12.0"
  }
}
